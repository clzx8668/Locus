# Tasks
- [x] Task 1: 将 AI 回复气泡底部按钮从文字改为图标
  - 在 `_buildConversationBubble` 的 AI 消息分支中，将三个 `_miniTextButton(...)` 替换为 `_BlockIconButton`
  - 图标：复制 `Icons.copy_outlined`、导出 `Icons.ios_share_rounded`、更多 `Icons.more_horiz_rounded`
  - 保持现有间距和缩进不变
  - 移除不再被使用的 `_miniTextButton` 方法

# Task Dependencies
- Task 1 无依赖
